CREATE TABLE ACTIVE_CHAT (ID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,CHAT_ID BIGINT);
CREATE TABLE INCOME (ID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,CHAT_ID BIGINT,INCOME INTEGER);
CREATE TABLE SPEND (ID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY NOT NULL,CHAT_ID BIGINT,SPEND INTEGER);